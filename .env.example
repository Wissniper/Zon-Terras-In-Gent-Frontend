VITE_MAPBOX_TOKEN=pk.your_mapbox_public_token_here

# API target for the Vite dev server proxy.
# Leave unset to use the production API (https://api.sun-seeker.be).
# Set to http://localhost:3000 to use a local backend.
VITE_API_URL=
